import { Container } from "@mui/material";
import React from "react";

const Footer = () => {
  return (
    <Container
      maxWidth="off"
      sx={{ backgroundColor: "black", color: "white", height: 500 }}
    ></Container>
  );
};

export default Footer;
